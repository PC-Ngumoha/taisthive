import { Inter, Borel, Shantell_Sans } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const borel = Borel({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
})

export const shantell_sans = Shantell_Sans({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
})