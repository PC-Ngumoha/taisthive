'use strict';
/**
 * app.js 
 * 
 * Contains main API code.
 */
const express = require('express');
const app = express()


app.listen(3000, () => {
  console.log('Server up and running at port 3000')
});